[package]
name = "geo-plugin-carbon"
version.workspace = true
description = "Carbon accounting engine: IPCC emission factor method, LCA via brightway2, remote sensing carbon sink"
edition.workspace = true

[dependencies]
geo-core = { path = "../../core/geo-core" }
geo-registry = { path = "../../core/geo-registry" }
geo-carbon-math = { path = "../../core/geo-carbon-math" }
geo-emission-factors = { path = "../../core/geo-emission-factors" }
toml.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["rt"] }
