[package]
name = "geo-plugin-ecology"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "生态修复评估插件 — NDVI 变化检测、碳汇计算、植被恢复评估"

[dependencies]
geo-core = { path = "../../core/geo-core" }
geo-carbon-math = { path = "../../core/geo-carbon-math" }
geo-raster = { path = "../../core/geo-raster" }
geo-stats = { path = "../../core/geo-stats" }
geo-io = { path = "../../core/geo-io" }
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
thiserror.workspace = true
chrono.workspace = true

# ⚠ 注意：不依赖 geo-plugin-carbon（保持 Plugin 零横向依赖）
# ⚠ 注意：不依赖任何 geo-adapter-*
